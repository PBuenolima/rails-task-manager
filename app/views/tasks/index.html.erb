<div class="container">
  <h1 class="fw-100"> Tasks </h1>

  <ul class='task_list'>
    <% @tasks.each do |task| %>
    <li> <%= link_to task.title, task_path(task) %> </li>
    <% end %>
  </ul>

  <%= link_to 'New task', new_task_path, class: 'btn btn-primary p-3' %>
</div>
